#!/bin/bash

# this starts a php server to host your powershell payload 
# -s for web server
# 0.0.0.0:80 it listens on all of the interfaces on port 80
# -t to specify directory to host which is ~ (home dir) in this case
# sudo php -S 0.0.0.0:80 -t ~
